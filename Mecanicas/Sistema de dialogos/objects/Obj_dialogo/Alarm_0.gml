if inicializar == true{
if caractere < string_length(texto_grid[# Infos.Texto, pagina]){
	//var _snd = Sound1 // alterar pra o dialogo original
	//audio_play_sound(_snd, 1, 0)
	caractere ++
	alarm[0] =1;
}}